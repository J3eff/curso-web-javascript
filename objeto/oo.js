// CÓDIGO NÃO EXECUTAVEL !!! - CONCEITOS DA OO

// Paradigma procedural -> é baseado no procedimento/procedure/função

// Procedural  
processamento(valor1, valor2, valor3)

// OO
object = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}
object.processamento() // Foco passou a ser o objeto.

// Principios importantes:
// 1. abstração -> abstrair algo do mundo real.
// 2. encapsulamento -> e você ter os detalhes de implementação escondido e mostrar só para quem precisa usar.
// 3. herança (protype) -> não aceita heranças multiplas 
// 4. polimorfismo -> multiplas forma
 